- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc:
     ~/.gitconfig:
- create:
    - ~/Work
    - ~/downloads
 
- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true

