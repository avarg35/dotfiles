- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc:
     ~/.gitconfig:
- create:
    - ~/Work
    - ~/downloads
 
# - shell:
#   - [git submodule update --init --recursive, Installing submodules]

